<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectobuscan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".AnimalesAdopcionActivity" />
        <activity android:name=".RegistrarUsuarioActivity" />
        <activity android:name=".CambioClaveActivity" />
        <activity android:name=".RegistrarMascotaActivity" />
        <activity android:name=".DonemosActivity" />
        <activity android:name=".MascotasEncontradasActivity" />
        <activity android:name=".MiPerfilActivity" />
        <activity android:name=".LoginBusCan" />
        <activity android:name=".MainActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PuntosRecompensasActivity" />
        <activity android:name=".AnimalesAdoptadosActivity" />
        <activity android:name=".EstadoMascotaActivity" />
    </application>

</manifest>